# MongoDB connection string (Optional: if not provided or pointing to localhost, runs in-memory fallback database)
MONGODB_URI=

# JWT Secret key for signing authorization tokens
JWT_SECRET=

# Resend API Key for sending emails (Optional)
RESEND_API_KEY=

# Frontend API base URL for production deployments
VITE_API_BASE_URL=/api
